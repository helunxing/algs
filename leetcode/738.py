class Solution:
    def monotoneIncreasingDigits(self, N: int) -> int:
        tmps = ''
        n = str(N)
        if len(n) == 1:
            return N

        for i in range(1,len(n)):
            pass
